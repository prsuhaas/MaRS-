Reaction Time Tester

The system waits for a random delay before turning on an LED, after which the user must press a button as quickly as possible. The reaction time is calculated using the difference between two millis() readings. Early button presses are detected and flagged as invalid. This project demonstrates event timing and user interaction using digital inputs.